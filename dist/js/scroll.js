$(function() {
  $.scrollify({
    section: ".scrollable",
    sectionName: false,
    easing: "easeOutExpo",
    scrollSpeed: 1300,
    offset: 0,
    scrollbars: true,
    standardScrollElements: "", // Elementos que precisar de Scroll normal.
  });
});